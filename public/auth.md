# Auth.md

## Agent Registration
RedCheck uses pre-provisioned OAuth 2.0 credentials. Automated dynamic agent registration is currently not supported. 

## Authentication
To interact with the endpoints programmatically, reference the metadata at `/.well-known/oauth-authorization-server`.